# 開発用Dockerfile（ホットリロード対応）
FROM golang:1.24

WORKDIR /app

# airをインストール（ホットリロード用）
RUN go install github.com/air-verse/air@v1.61.7

# go.mod/go.sumをコピー
COPY go.mod go.sum ./
RUN go mod download

# ソースコードをコピー
COPY . .

# ポートを公開
EXPOSE 8080

# airでホットリロード実行
CMD ["air", "-c", ".air.toml"]
